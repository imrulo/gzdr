# Clerk (opcional). Si no usas Clerk, deja estos valores vacíos y la app funciona igual.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Stripe (opcional, modo test). Requerido solo si activas el checkout real en `src/app/api/checkout/route.ts`.
STRIPE_SECRET_KEY=

# URL pública del sitio (útil para Stripe success/cancel URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
